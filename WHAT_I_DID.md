# build-fable — 何を作ったか

ワールド寺子屋プログラム2026 LP の「扉」版。
ロゴの E は扉の枠、d には扉、マスコットも扉。その「扉」を主役にして、
**筋ガラスの扉の向こうに現地の映像が見え、スクロールで扉を押し開けて向こう側へ歩いていく** FV を中心に、
ページ全体を作り直した。到達目標は alche.studio と同じ「主役のオブジェクトが章ごとに性格を変えて生き続ける」水準。

起動: `cd build-fable && python3 -m http.server 8961` → http://127.0.0.1:8961/
（ES Modules と fetch を使うので `file://` では動かない）

---

## どこを見ればよいか（見どころの順）

| # | 場所 | 何が起きるか |
|---|------|------------|
| 1 | **幕 → FV** | ロゴマークの幕が開くと、閉じていた扉がバネで少し開き、見出しが1文字ずつ持ち上がる |
| 2 | **FV の扉（WebGL）** | **筋ガラス（縦の溝）の扉**。溝ごとに背景の映像がずれ、縁に **虹（分散）** が走る。マウスで扉が少し押され、視線も追従する。ノブは黄。床に光がこぼれ、枠が床に映る |
| 3 | **スクロール（FV 280vh）** | 扉が押し開く → カメラが扉をくぐる → 開口の向こうの映像（現地校の廊下を歩く実写）がそのまま全画面になる。通り抜けの間は上部バーとレールが引っ込む |
| 4 | **01 流れ** | 章の中央（SP は左端）を **マスコットがスクロール量ぶん歩き、着地のたびに虹の足あとを置く**。足あとはページに残る。日本→現地で地の色が変わる |
| 5 | **02 3つの扉** | 3派遣が **CSS 3D の扉カード**。ホバーで少し開き、押すと 106° 開いて中の写真と本文が現れ、下に事実が展開する |
| 6 | **03 あなたへ** | 名札タブ。下線がバネ（`linear()` 焼き込み）で滑る |
| 7 | **04 費用** | 55 / 66 / 77 万円が画面に入るとカウントアップ |
| 8 | **05 説明会** | 3枚のチケット。ホバーで傾く |
| 9 | **最終CTA** | 固定キャンバスの扉がもう一度、足元に。今度は開いていて、マスコットが一礼する |
| 10 | **PC のカーソル** | 黄の点＋輪。リンクで開き、扉カードの上で「OPEN / CLOSE」、FV で「SCROLL」に化ける |
| 11 | **FV のマスコット** | 瞳がカーソルを追い、近づくと口が動いて喋る。瞬きあり |

---

## 技術

### 扉（`js/door.js` ＋ `js/lib/`）— 依存ゼロの WebGL2

three.js を持ち込むと 2MB になるので、扉ひとつのために必要な分だけ自前で書いた（行列・バネ・ジオメトリ・FBO で約 1,100 行、ライブラリ 0）。

alche.studio の解析（`~/Desktop/Web制作/サイト再現・クローン/alche-study/_analysis/TECH_REPORT.md`）から持ち帰った技法:

1. **背景スナップショットの取り込み** — ガラスより先に描いた画面を 640px に縮小コピーし、ガラスはそれを法線でずらして読む（物理屈折は計算しない）
2. **R/G/B 別のずらし量 = 分散** — 1 : 2 : 4 の比。プリズムの虹の正体。プライドフラッグ（ロゴの足あと）への目配せ
3. **乱数ジッタ × 8 サンプル = すりガラス**
4. **GGX ハイライト + Schlick フレネル**

このLP独自の追加:

5. **筋ガラス** — パネルのローカル x で法線を周期的に傾ける。閉じていても虹の縦縞が走り、開くと溝が斜めに流れる
6. **ステンシルの「扉の向こう」** — 開口の矩形をステンシルに書き、映像の板はその内側だけに描く。映像の板は毎フレーム「今のカメラから画面を覆う大きさ」に合わせるので、映像は常に画面いっぱいの構図で固定され、扉の開口だけが近づくにつれ広がる。通り抜けた瞬間にそのまま全画面になる
7. **MSAA 4x のオフスクリーン → 解決 → 縮小コピー → ガラス → 合成（粒子・ビネット）** の順で 1 フレームを作る
8. **床への映り込み** — 枠とノブの鏡像を薄く重ねる（ガラスは屈折が破綻するので除く）
9. **動きはバネと半減期** — 扉の開き・カメラのドリー・視線の追従は `Spring.fromFeel({settle, overshoot})` と `expSmooth(halfLife)`。フレームレート非依存。スクロール進捗は半減期 55ms で平滑化
10. **章ごとの性格** — `fv`（触れる・通り抜ける）と `final`（足元に開いて立つ）。1つのキャンバスを `position:fixed` で置き、透明な章の裏でだけ描画する（不透明な章の間は rAF を止める）

### マスコット（`js/mascot.js`）

`assets/mascot/rig-rev.svg`（部品ごとに `id` と `transform-origin` を持つリグ）を fetch して `<img>` と差し替える。

- 顔: 瞳がカーソルを追う（半減期 0.09s）、顔はゆっくり（0.22s）、口は 90〜220ms の不規則な間隔（正弦波にすると機械の口になる）
- 歩き: `phase += dy / 96px`。着地は sin = ±1 の位相で、置く位置は「いま着地した靴」の実座標から取る（`README.md` の4条件）
- おじぎ: `#torso` をバネで 14° → 0°
- 規則: `transform-origin` を持つ要素は `style.transform` で回す（属性と併用しない）。`#trail` は隠す

### ページ側（`js/ui.js` / `js/main.js` / `css/`）

- 見出しの文字分割（`[data-split]`）・出現（IO + 高速スクロールの保険 sweep）・写真は **mask-size** で拭き上げ（clip-path は IO に検知されない）
- 章の色: 上部バーの真下にある要素で `is-light` を切り替える
- 扉カード・FAQ の開閉は `grid-template-rows: 0fr → 1fr`。閉じ切ってから `hidden` を戻す
- マグネットボタン（バネ・PC のみ）、チケットの傾き、カーソル
- 動画は IO で `data-src` を差し込み、画面内だけ再生

### 自己完結

- 書体は **自己ホスト・サブセット**（`fonts/build_fonts.py`。描画面から実使用文字を採取して Google Fonts の `text=` で 9 face を落とす。合計 283KB。外部リクエストは 0）
- three.js 等のライブラリなし。全部このフォルダの中
- 動画は 4K 元素材から 720p / 540p に再エンコード（`video/`）。door.mp4 2.5MB、他 0.9〜1.3MB

### 守ったこと

- `prefers-reduced-motion: reduce`: 扉の揺れ・粒子・歩き・顔・マグネット・出現アニメを止め、映像も再生しない
- JS 無効: 映像の1コマ目を FV の背景に敷き、全文がそのまま読める（37/37 要素表示を確認）
- WebGL 無し: CSS で作った扉に同じ `<video>` を入れる。FV は 1 画面になる
- 幕は最短 520ms・最長 1.9s。リロードの位置復元は幕が開くまで押さえる
- 5 種の検証をヘッドレス Chromium（`--use-angle=metal`）で実行: 通常 / JS 無効 / WebGL 無効 / reduced-motion / 描画時間（1280×800 で rAF 間隔 中央値 8.3ms）
- 幅は 375（SE）/ 390 / 1280 で FV・通り抜け・各章・最終CTA・フッターまで目視。console エラー 0・横スクロール 0
- ブラウザは常に 1 インスタンスずつ起動して閉じた（並列起動なし）

---

## ファイル

```
index.html
css/base.css        トークン・リセット・共通部品
css/fv.css          幕・舞台・上部バー・レール・FV・カーソル・追従CTA
css/sections.css    01 流れ / 02 扉 / 03 あなたへ / 04 費用 / 後援 / 05 説明会 / FAQ / 最終 / フッター
js/main.js          起動・スクロール進捗・章の色・結線
js/door.js          扉の WebGL2 シーン
js/mascot.js        マスコット（顔・歩き・おじぎ）
js/ui.js            タブ・開閉・出現・カウント・動画・マグネット・傾き・カーソル・計測
js/lib/gl.js        WebGL2 の道具（プログラム・VAO・MSAA FBO）
js/lib/mat4.js      行列・ベクトル
js/lib/spring.js    バネ・半減期
js/lib/geom.js      面取り箱・球・円柱・板
fonts/              自己ホスト woff2 ＋ 再生成スクリプト
assets/mascot/      リグ（暗い地用のキーライン付き `rig-rev.svg`）・ポーズ・足あと
video/ img/         再エンコード済み動画とポスター・写真
```

## 既知の制約・次にやるなら

- 申込フォームの URL は未確定（`href="#briefing"` で説明会の章へ。`data-todo="form-url"` の2箇所は `#`）
- 扉の分散の強さ・溝の幅は `door.js` の `render()` 内の uniform 5 行で調整できる（`uFlute` / `uReedK` / `uRefract` / `uDisp` / `uRough`）
- ガラスの映り込みは床に描いていない（枠とノブだけ）。描くなら鏡像用にもう一度スナップショットが要る
- スマホの実機（iOS Safari）では未確認。ヘッドレス Chromium の 375 / 390 / 1280 幅で確認済み
